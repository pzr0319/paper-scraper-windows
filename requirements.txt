# ── Core (both scrapers) ──────────────────────────────────────────────────────
curl_cffi>=0.7.0          # Chrome TLS fingerprint spoofing
browser-cookie3>=0.19.0   # Read cookies directly from local Chrome

# ── INFORMS scraper ───────────────────────────────────────────────────────────
beautifulsoup4>=4.12.0    # HTML parsing
lxml>=5.0.0               # Fast HTML parser backend for BeautifulSoup

# ── Optional: Excel output ────────────────────────────────────────────────────
openpyxl>=3.1.0           # .xlsx export (both scrapers)

# ── Optional: ScienceDirect DevTools PDF download ────────────────────────────
websocket-client>=1.7.0   # CDP websocket (sd_scraper DevTools mode)

# ── Optional: ScienceDirect Playwright PDF download (alternative method) ─────
# playwright>=1.40.0      # Only needed for download_pdfs_via_chrome()
#                         # Install: pip install playwright && playwright install chromium
